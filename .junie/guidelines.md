<!--
Этот документ — рекомендации для Junie (JetBrains AI) и всех участников проекта.
Фокус: учебный процесс, простота, максимум поясняющих комментариев.
-->

# 📋 Development Guidelines для Junior Developer

---

## 🧱 Стек технологий

* **PHP 8.4 (FPM)**
* **Nginx** (балансировщик и backend)
* **Redis Cluster** (для хранения сессий и сбора статистики)
* **Docker / Docker Compose**
* **Composer** (PSR-4 autoload)
* **Vue 3 + Vite + Axios**

> **Важно!**
> Приложение использует внешний Redis Cluster, запущенный на множестве контейнеров через Docker Compose.
> Пример структуры контейнеров:
>
> ```
> CONTAINER ID   IMAGE             ...   PORTS                  NAMES
> df43cd079497   redis:7.2-alpine  ...   0.0.0.0:7007->6379/tcp redis-cluster-redis-node7-1
> ...
> ```
>
> Пример docker-compose.yml для Redis Cluster смотри в корне проекта.

---

# Детальный план разработки сервиса EmailПроверка+

> **Текущий статус проекта:**
> Базовая архитектура реализована. Все основные классы созданы и функционируют:
> - EmailController с множественными методами валидации
> - EmailVerificationService для обработки массивов email
> - EmailValidator с композитной структурой валидаторов
> - SyntaxValidator, TldValidator, MxValidator
> - ValidationResult как DTO для результатов
> - Router для маршрутизации запросов
> - RedisHealthChecker для мониторинга инфраструктуры
> 
> Дальнейшие шаги фокусируются на улучшении и расширении функциональности.

## 1. Архитектура и задание задачи ✓

* ✓ Определить основные требования к сервису (валидация email, поддержка современных TLD, MX-проверка, отсутствие жёстких ограничений).
* ✓ Выбрать подходящую структуру классов (EmailValidator, EmailController, EmailVerificationService).
* ✓ Спроектировать интерфейс (на вход — массив email-строк, на выход — массив статусов и причин).
* ✓ Реализованы все основные классы архитектуры согласно описанию выше.

Вот **подтверждённая и финализированная структура классов** для сервиса EmailПроверка+ с объяснением ролей и взаимодействия:

---

## Архитектура приложения (от фронта до ядра)

### 1. **Vue Frontend**

* Пользовательский интерфейс для ввода email.
* Отправляет POST-запрос с массивом email на backend (`/verify`).
* Отправляет GET-запрос на `/status` для мониторинга состояния Redis-кластера.

### 2. **public/index.php** (Точка входа)

* Центральный входной файл приложения в директории `public/`.
* Загружает автолоадер Composer.
* Инициализирует маршрутизатор (**Router**).
* Регистрирует маршруты:
  * `POST /verify` → EmailController::validateEmails()
  * `GET /status` → RedisHealthChecker (через анонимную функцию)
* Запускает диспетчеризацию запроса через `$router->dispatch()`.
* Устанавливает CORS заголовки и обрабатывает preflight запросы.

### 3. **Router**

* Управляет таблицей маршрутов (путь + HTTP-метод + обработчик).
* Выбирает подходящий обработчик для входящего запроса (например, вызывает метод контроллера).
* Обеспечивает “переадресацию” между URL и бизнес-логикой.

### 4. **EmailController**

* Обработчик для маршрута `/verify` (POST).
* Получает данные из запроса через метод `getRequestData()`.
* Может использовать **ValidationRequest** для проверки и парсинга входных данных (опционально).
* Вызывает **EmailVerificationService** для выполнения основной логики валидации email.
* Имеет множественные методы: `validateEmails()`, `verifyWithStats()`, `verifySingle()`, `verifyWithConfig()`.
* Формирует и возвращает JSON-ответ клиенту через методы `sendSuccessResponse()` и `sendErrorResponse()`.

### 5. **ValidationRequest**

* Валидирует и парсит входные данные запроса (например, проверяет наличие и тип поля, длину текста).
* Преобразует входящий текст во внутренний формат (например, массив email).

### 6. **EmailVerificationService**

* Принимает на вход массив email.
* Для каждого email вызывает **EmailValidator**.
* Собирает и возвращает массив объектов **ValidationResult**.

### 7. **EmailValidator**

* “Композит”, который поочерёдно вызывает все этапные валидаторы:

   * **SyntaxValidator** — синтаксис email.
   * **TldValidator** — актуальность домена (TLD) по IANA.
   * **MxValidator** — наличие MX-записи.
* Возвращает первый найденный результат с ошибкой (“fail-fast”) или статус valid.

### 8. **ValidatorInterface**

* Интерфейс, который реализуют все этапные валидаторы (`validate($email): ValidationResult`).

### 9. **ValidationResult**

* DTO: email, статус (`valid`, `invalid_format`, `invalid_tld`, `invalid_mx`), причина.

---

### **Дополнительно (инфраструктурно):**

#### **RedisHealthChecker**

* Вспомогательный сервис, не связанный с валидацией email.
* Проверяет состояние Redis-кластера (маршрут `/status`).
* Используется для мониторинга доступности Redis на фронте.
* Не участвует в бизнес-логике, но нужен для инфраструктуры и “здоровья” сервиса.

---

## Описание классов и ролей

| Класс                        | Назначение                                                           | Взаимодействие                 |
|------------------------------|----------------------------------------------------------------------|--------------------------------|
| **index.php**                | Главная точка входа, инициализация приложения, маршрутизация         | Router, контроллеры            |
| **Router**                   | Определяет, какой обработчик вызвать по URL и методу                 | Контроллеры                    |
| **EmailController**          | Получает данные запроса, вызывает сервис, возвращает JSON-ответ      | ValidationRequest, Service     |
| **ValidationRequest**        | Валидация и обработка входных данных запроса                         | Используется в EmailController |
| **EmailVerificationService** | Ядро бизнес-логики — вызывает EmailValidator для массива email       | EmailValidator                 |
| **EmailValidator**           | Поочерёдно вызывает все валидаторы                                   | Syntax/Tld/MxValidator         |
| **ValidatorInterface**       | Интерфейс для всех этапных валидаторов                               | Имплементируется валидаторами  |
| **DomainValidatorInterface** | Интерфейс для валидаторов доменной части email                       | Имплементируется валидаторами  |
| **PartsValidatorInterface**  | Интерфейс для валидаторов частей email                               | Имплементируется валидаторами  |
| **SyntaxValidator**          | Проверка синтаксиса email                                            | Implements ValidatorInterface  |
| **TldValidator**             | Проверка TLD email по IANA                                           | Implements ValidatorInterface  |
| **MxValidator**              | Проверка MX-записи домена                                            | Implements ValidatorInterface  |
| **ValidationResult**         | Хранит результат проверки email                                      | Используется всеми выше        |
| **RedisHealthChecker**       | Проверка состояния Redis-кластера (инфраструктура, не бизнес-логика) | Используется только по /status |

---

## Общая схема запроса

```
[Vue Frontend]
      |
      v
 [HTTP-запрос (POST /verify)]
      |
 [public/index.php]
      |
 [Router]
      |
 [EmailController]
      |
 [getRequestData() / ValidationRequest]  (обработка данных)
      |
 [EmailVerificationService]
      |
 [EmailValidator]
      |
 [SyntaxValidator, TldValidator, MxValidator]
      |
 [ValidationResult]
      |
 [EmailController]
      |
 [Ответ JSON во Frontend]
```

---

## Пример маршрутов

* **POST /verify** → EmailController::validateEmails()
* **GET /status** → RedisHealthChecker (через анонимную функцию в index.php)

---

## Кратко: RedisHealthChecker включать?

**Включать только в качестве вспомогательного инфраструктурного компонента.**
В архитектуре бизнес-логики валидации email — не участвует, но важен для здоровья и мониторинга всего приложения.

**Это финальное подтверждение классов, их ролей и взаимодействия.**
Дальше — можно расписать сигнатуры методов каждого класса или перейти к детальной проработке первого этапа (синтаксическая валидация).


## 2. Синтаксическая валидация email (RFC 5322)

* Исследовать и выбрать регулярное выражение, максимально совместимое с RFC 5322.
* Реализовать функцию валидации синтаксиса (например, через регулярку и/или `filter_var`).
* Покрыть тестами граничные случаи: спецсимволы, quoted-string, подчеркивания, длинные локальные и доменные части.

## 3. Поддержка современных TLD (динамический список)

* Реализовать загрузку и парсинг списка TLD с [https://data.iana.org/TLD/tlds-alpha-by-domain.txt](https://data.iana.org/TLD/tlds-alpha-by-domain.txt)
* Организовать кэширование или локальное хранение списка на случай недоступности IANA.
* Обеспечить независимость от жёстко заданного списка.
* Обеспечить сравнение TLD без учёта регистра.

## 4. Проверка существования TLD

* Извлекать TLD из email и сверять с загруженным списком.
* Добавить обработку edge-case'ов (сложные домены, вложенные субдомены и пр.).
* Покрыть тестами реальные и несуществующие TLD.

## 5. Проверка MX-записи у домена

* Реализовать проверку MX-записи с помощью `checkdnsrr` (или аналога).
* Продумать обработку ошибок DNS (timeout, network error).
* Протестировать домены с рабочими и нерабочими MX.

## 6. Поддержка IPv6 и особых доменных случаев

* Корректно обрабатывать email с IPv6 адресами (например, user@\[IPv6:...]).
* Протестировать edge-cases (поддомены, кириллица/IDN).

## 7. Интерфейс и структура вывода

* На вход — массив email-строк.
* На выход — массив, где каждый email содержит статус и причину (valid, invalid\_format, invalid\_tld, invalid\_mx).
* Оформить понятные сообщения ошибок на русском языке.

## 8. Покрытие тестами

* Составить набор тест-кейсов по всем видам адресов (валидные, невалидные, экзотические TLD, отсутствие MX, синтаксические ошибки).
* Автоматизировать прогон тестов (PHPUnit).

## 9. Оптимизация и отказоустойчивость

* Внедрить кэширование списка TLD.
* Настроить fallback-стратегию, если IANA недоступна.
* Настроить таймауты и обработку ошибок DNS.

## 10. Документация

* Описать все этапы валидации в README и/или inline-комментариях.
* Добавить инструкции по использованию сервиса, обновлению TLD и интерпретации ошибок.

---

**Дальнейшие шаги:**

* Начать с первого пункта, после реализации — переходить к следующему, постепенно формируя полноценный, надёжный и расширяемый модуль.
* После каждого этапа — тестировать функциональность и покрытие кейсов.
