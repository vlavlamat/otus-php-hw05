<!--
Этот документ — рекомендации для Junie (JetBrains AI) и всех участников проекта.
Фокус: учебный процесс, простота, максимум поясняющих комментариев.
-->

# 📋 Development Guidelines для Junior Developer

---

## 🧱 Стек технологий

* **PHP 8.4 (FPM)**
* **Nginx** (балансировщик и backend)
* **Redis Cluster** (для хранения сессий и сбора статистики)
* **Docker / Docker Compose**
* **Composer** (PSR-4 autoload)
* **Vue 3 + Vite + Axios**

> **Важно!**
> Приложение использует внешний Redis Cluster, запущенный на множестве контейнеров через Docker Compose.
> Пример структуры контейнеров:
>
> ```
> CONTAINER ID   IMAGE             ...   PORTS                  NAMES
> df43cd079497   redis:7.2-alpine  ...   0.0.0.0:7007->6379/tcp redis-cluster-redis-node7-1
> ...
> ```
>
> Пример docker-compose.yml для Redis Cluster смотри в корне проекта.

---

## 🎯 Основной принцип: Keep It Simple, Student (KISS)

**Этот проект — учебный. Цель: понять основы, а не построить enterprise. Простые решения всегда лучше сложных!**

---

## ✅ Что МОЖНО и НУЖНО делать

### 🏗️ Архитектура

* Следовать требованиям ТЗ максимально точно.
* Использовать самые простые решения для простых задач.
* Применять принципы SOLID и паттерны только там, где это явно делает код лучше.
* Писать код с максимально понятными названиями переменных и функций.
* **Использовать архитектурный стиль REST API для взаимодействия между frontend и backend.**

    * Все endpoints должны быть чистыми, предсказуемыми, оформленными по REST-подходу (`/api/verify`, `/api/status` и т.д.).
    * Не использовать RPC, GraphQL или SOAP для этого проекта.
    * Ответы API должны быть структурированы в JSON.
* Соблюдать единые правила нейминга (snake\_case или camelCase — выбрать и следовать по всему проекту).

### 🔧 Технические решения

* Использовать только стандартные подходы (никаких самописных фреймворков).
* Минимизировать зависимости.
* Реализовывать базовую обработку ошибок (try/catch), без сложной системы логирования.
* Тестировать только основную бизнес-логику простыми тестами.

### 📝 Документация и комментарии

* **Писать комментарии буквально ко всему:**

    * К каждой функции, переменной, строке кода — объяснять, что и зачем происходит.
    * Если есть сомнения — лучше написать комментарий даже к очевидной вещи.
    * Раскрывать, почему выбрано то или иное решение, даже если оно кажется тривиальным.
* Не бояться избыточных пояснений — цель этого проекта: обучение и закрепление знаний.
* Краткий и понятный `README.md` с инструкцией по запуску и описанием логики.
* Краткое описание, какие модули и части системы за что отвечают.

---

## ❌ Что НЕ НУЖНО делать (Overengineering)

### 🚫 Архитектурные излишества

* Не использовать Domain Driven Design для простых задач.
* Не внедрять Event Sourcing для тривиальных операций.
* Не разбивать приложение на микросервисы, если хватает монолита.
* Не создавать абстракций "на будущее" (YAGNI).

### 🚫 Технические излишества

* Не внедрять очереди для синхронных операций.
* Не писать свои ORM или DI-контейнеры.
* Не кешировать простые вычисления.
* Не использовать сложные паттерны (Strategy, Factory, Observer) без реальной необходимости.

### 🚫 Инфраструктурные излишества

* Не настраивать CI/CD pipeline для домашних заданий.
* Не внедрять monitoring/alerting.
* Не создавать множество окружений (dev/staging/prod).
* Не делать нагрузочное тестирование для учебного проекта.

### 🚫 Frontend излишества

* Не использовать state management (Vuex/Pinia) для простых форм.
* Не подключать большие component libraries ради 2-3 компонентов.
* Не делать SSR/SSG для статического контента.
* Не усложнять валидацию на frontend.

---

## 🎯 Критерии принятия решений

Перед добавлением любой функциональности спроси себя:

1. **Требует ли это ТЗ?** — Если нет, скорее всего это не нужно.
2. **Решает ли это реальную проблему проекта?** — Не гипотетическую.
3. **Можно ли решить проще?** — Простое решение всегда лучше.
4. **Потрачу ли я на это больше часа?** — Если да, подумай дважды.

---

## 📚 Фокус на обучении

Вместо усложнения сосредоточься на:

* **Понимании основ:** Как работает HTTP, PHP, Docker, Nginx.
* **Качестве кода:** Читаемость важнее архитектурных изысков.
* **Отладке:** Учись находить и исправлять ошибки.
* **Документировании:** Опиши, что делает код и почему — подробные комментарии обязательны!
* **Комментариях:** Каждый фрагмент должен быть понятен даже без знания всей структуры проекта. Комментарии — это не «опция», а must-have.

---

## 🚨 Red Flags (Когда остановиться)

Остановись, если ловишь себя на мыслях:

* "А что если нам понадобится..."
* "В production так делать нельзя..."
* "Давай добавим еще один слой абстракции..."
* "Это не enterprise-ready..."

---

## 🎉 Помни главное

**Лучший код для учебного проекта — это код, который:**

* Работает по ТЗ
* Легко читается
* Легко запускается
* Решает поставленную задачу
* Содержит максимум обучающих комментариев

**Худший код — это тот, который:**

* Решает несуществующие проблемы
* Требует лишних технологий
* Сложнее самой задачи
* Не содержит объясняющих комментариев

---

> **Главное правило: «Если проще — значит лучше. Если понятно даже из комментариев — значит отлично!»**

Готово!
Весь итоговый функционал фронта и бэка теперь объединён и структурирован в одном документе (Canvas):

---

# 🟦 Функционал фронтенда (email-verifier)

1. **Окно (textarea) для вставки текста**

  * Пользователь может вставить любой текст — ограничение: до **15 000 символов**.
  * Окно рассчитано на 20 строк — если текста больше, появляется вертикальная прокрутка.
  * **Проверка лимита на фронте:** если пользователь превысил 15 000 символов, выводится предупреждение, и отправка текста на backend блокируется.

2. **Кнопка «Проверить»**

  * После нажатия (если не превышен лимит) весь введённый текст отправляется на backend через API одним куском, без предварительной обработки и разделения на email-адреса.

3. **Вывод результата**

  * После проверки напротив каждого найденного email отображается статус:

    * Жирная зелёная галочка — email валиден
    * Жирный красный крест — email невалиден
  * UI не ломается даже при большом числе email (до лимита по символам).

4. **Юзабилити и UX**

  * Если email-ов много (30–40+), появляется прокрутка справа.
  * Интерфейс остаётся быстрым и удобным даже для больших списков.

---

# 🟩 Функционал backend (email-verifier)

1. **Получение данных**

  * Backend принимает "сырое" текстовое поле (любой текст) от фронта через API.
  * Лимит на размер входящего текста — **15 000 символов**.

2. **Выделение email-адресов**

  * На бэке выполняется разбор текста:

    * Используется глобальное регулярное выражение для поиска всех потенциальных email-адресов (работает даже при отсутствии разделителей, для “слипшихся” email-ов).
    * Разделители: перевод строки, запятая, точка с запятой, пробел, а также любые слипшиеся email по regex.

3. **Валидация email-адресов**
   Для каждого найденного email:
   \- Проверка синтаксиса (через filter\_var с флагом FILTER\_VALIDATE\_EMAIL).
   \- Проверка существования MX-записи у домена (checkdnsrr).
   \- (Опционально) Проверка TLD по актуальному списку IANA для исключения "фантазийных" зон.

4. **Формирование и возврат результата**

  * Backend возвращает список найденных email с их статусами:

    * valid — email валиден (по синтаксису и MX)
    * invalid\_format — некорректный формат
    * invalid\_mx — корректный формат, но не найден MX
    * invalid\_tld — TLD отсутствует в списке IANA
  * Сообщение об ошибке, если превышен лимит (15 000 символов)

5. **UI**

  * Фронтенд отображает статус для каждого email согласно ответу бэка

---

**Если потребуется ещё что-то отредактировать или расширить — просто скажи!**

